@model IEnumerable<WebApplicationSistemaDeReclamo.Models.ViewModels.ReclamoViewModel>

@{
    ViewData["Title"] = "Listado de reclamos";
}

<h1>Listado de reclamos</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form action="/Reclamos/Buscar" class="row row-cols-lg-auto align-items-center">
  <div class="col-6">
      <input type="text" value="@ViewBag.textoBuscar" name="textoBuscar" class="form-control" placeholder="Buscar reclamos">
  </div>

  <div class="col-6">
    <button type="submit" class="btn btn-primary">Buscar</button>
  </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaAlta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaAlta)
            </td>
            <td class="d-flex justify-content-between">
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class="btn btn-secondary" }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class="btn btn-secondary" }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class="btn btn-secondary", onclick="borrarReclamo(this); return false;" })
            </td>
        </tr>
}
    </tbody>
</table>

<script>
    function borrarReclamo(linkActual){
        console.log(linkActual)
        bootbox.confirm('Estás seguro de eliminar este reclamo?', function (result) {
            if(result == true){
                window.location = linkActual.href;
            }
        });
    }
</script>